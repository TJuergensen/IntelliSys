Vorgehen Daten"analyse":
    Jede Markierung ist Koordinate, die ein Objekt markieren (Objekt selbst ist größer)
    Näheres Umfeld betrachten -> Ausdehnung -> plotten? (HEATMAP! Hier aber kein Unterschied zw. A, B sichtbar)
    
    minima, maxima, mittelwerte, median

    


split in trainings und testdaten

Tipp: Nicht alle Daten verwenden, sondern teilen -> Liste teilen und später verwenden -> neue Daten
